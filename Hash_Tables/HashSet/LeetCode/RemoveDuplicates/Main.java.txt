import java.util.HashMap;
import java.util.List;

public class Main {
    public static void main(String[] args) {

        HashTable myHashTable = new HashTable();

        myHashTable.printTable();

        //Testing toc heck if remove duplicate method works and removes all the duplicates from the list
        System.out.println("Removing all the duplicates from the list: ");
        List<Integer> myList = new ArrayList<>(List.of(1, 2, 3, 4, 1, 2, 5, 6, 7, 3, 4, 8, 9, 5));
        System.out.println("The Numbers in array are: ");
        System.out.print("{ ");
        for(int i=0; i<myList.size(); i++){
            System.out.print(myList.get(i));
            if(i != myList.size()-1) System.out.print(", ");
        }
        System.out.print(" }");
        System.out.println();
        System.out.println("The list after removing the duplicates is: ");
        myList = myHashTable.removeDuplicates(myList);
        System.out.print("{ ");
        for(int i=0; i<myList.size(); i++){
            System.out.print(myList.get(i));
            if(i != myList.size()-1) System.out.print(", ");
        }
        System.out.print(" }");
        System.out.println();
        System.out.println();

   }

}
