public class Main {
    public static void main(String[] args) {
        DoublyLinkedList myDLL = new DoublyLinkedList(14);

        myDLL.printList();
        myDLL.getHead();
        myDLL.getTail();
        myDLL.getLength();

        // Testing to see if append functions works
        System.out.println("Adding new element at the end of the list: ");
        myDLL.printList();
        myDLL.append(7);
        myDLL.append(5);
        myDLL.append(8);
        myDLL.append(9);
        myDLL.printList();
        System.out.println();

        // Testing if the remove function works properly by removing the last element in the list
        System.out.println("Deleting element at the end of the list: ");
        myDLL.printList();
        System.out.println("The Node which was deleted: " + myDLL.removeLast());
        myDLL.printList();
        myDLL.getLength();
        System.out.println();

    }

}